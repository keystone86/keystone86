# Keystone86 / Aegis — Tooling and Observability Policy

## Purpose

This document defines the expected tooling interface, aggregate verification commands, and observability expectations for this project.

---

## Prerequisites

- `iverilog` and `vvp` (Icarus Verilog) — required for all RTL simulation targets
- `python3` — required for all script targets
- Run all commands from repo root

---

## Generated artifact policy

This repo has two categories of generated artifacts with different commit policies.

### Category 1 — Checked-in generated interface files

These files are generated from `tools/spec_codegen/appendix_a_codegen.json` and are **committed to the repo**:

- `rtl/include/keystone86_pkg.sv` — authoritative shared RTL package
- `rtl/include/*.svh` — legacy compatibility headers

They are committed because they define the RTL interface boundary. Every RTL module imports from `keystone86_pkg.sv`. Keeping them in version control makes the interface stable, reviewable, and available without running any tool.

Do not manually edit these files. If `appendix_a_codegen.json` changes, regenerate them with `make codegen` and commit the result.

### Category 2 — Local-only generated build products

These files are generated at build time and are **not committed to the repo**:

- `microcode/tools/generators/exports/*.inc` — microcode assembler export includes (generated by `make codegen`)
- `build/microcode/ucode.hex` — bootstrap microcode ROM (generated by `make ucode`)
- `build/microcode/dispatch.hex` — bootstrap dispatch table (generated by `make ucode`)
- `build/microcode/ucode.lst` — microcode listing (generated by `make ucode`)
- `build/microcode/dispatch.lst` — dispatch listing (generated by `make ucode`)

These must be created locally after a fresh clone. See **Fresh clone setup** below.

---

## Fresh clone setup

After cloning the repo for the first time, run these two commands before anything else:

```bash
make codegen   # generates microcode/tools/generators/exports/*.inc
               # also regenerates rtl/include/ if needed
make ucode     # generates build/microcode/*.hex and *.lst
```

**`make codegen`** must be run:
- once after a fresh clone (to create the local-only export includes)
- any time `tools/spec_codegen/appendix_a_codegen.json` changes

**`make ucode`** is run automatically as a dependency of all `rung*-sim` and `rung*-regress` targets, but may also be run manually at any time.

After running both, verify with:

```bash
make namespace-check       # confirms namespace export files are present
make ucode-bootstrap-check # confirms microcode ROM seed is consistent
```

---

## Aggregate verification commands

These are the commands that must pass before any handoff is considered valid.

### Full baseline (run in order after fresh clone setup)

```bash
make codegen
make ucode
make namespace-check
make ucode-bootstrap-check
make rung2-regress
```

`rung2-regress` is the comprehensive gate — it runs Rung 0 and Rung 1 baseline checks before running Rung 2. Passing `rung2-regress` with no failures means the complete current baseline is green.

### Abbreviated baseline check

If only confirming a single rung has not regressed:

```bash
make ucode && make rung{N}-sim
```

This is acceptable for targeted verification but does not replace the full regression chain before a handoff.

---

## All make targets

### Project checks

| Target | What it does |
|---|---|
| `make help` | Print all available targets |
| `make tree` | Print current repo tree |
| `make spec-check` | Verify frozen spec files exist and are non-placeholder |
| `make frozen-manifest-check` | Verify frozen spec manifest is complete |
| `make namespace-check` | Verify shared namespace/include/export files exist |
| `make spec-sync-status` | Show Appendix A import + codegen scaffold status |
| `make version-status` | Show versioning/release scaffold status |

### Build

| Target | What it does |
|---|---|
| `make ucode` | Build bootstrap microcode artifacts (`ucode.hex`, `dispatch.hex`, `ucode.lst`) |
| `make codegen` | Regenerate RTL includes and microcode export includes from Appendix A JSON |
| `make ucode-bootstrap-check` | Verify bootstrap microcode artifact consistency |
| `make ucode-clean` | Remove generated microcode artifacts |

### Smoke checks (host-side, no RTL simulation)

| Target | What it does |
|---|---|
| `make decode-dispatch-smoke` | Verify opcode-to-entry dispatch expectations |
| `make microseq-smoke` | Verify reset/dispatch/ENDI loop expectations |
| `make commit-smoke` | Verify commit mask and clear-fault behavior |
| `make service-abi-smoke` | Verify SVC/SVCW and SR_OK/WAIT/FAULT contract |
| `make prefetch-decode-smoke` | Verify byte consumption and ack/flush model |
| `make bootstrap-report` | Print bootstrap coverage summary |

### RTL simulation

| Target | What it does |
|---|---|
| `make rung0-sim` | Compile and run Rung 0 testbench |
| `make rung0-regress` | Run Rung 0 regression harness |
| `make rung1-sim` | Compile and run Rung 1 testbench |
| `make rung1-regress` | Run Rung 1 + Rung 0 baseline |
| `make rung2-sim` | Compile and run Rung 2 testbench |
| `make rung2-regress` | Run Rung 2 + Rung 1 + Rung 0 baseline |

### Cleanup

| Target | What it does |
|---|---|
| `make rung0-clean` | Remove Rung 0 build artifacts |
| `make rung1-clean` | Remove Rung 1 build artifacts |
| `make rung2-clean` | Remove Rung 2 build artifacts |
| `make clean` | Remove all generated files (ucode + all rung builds) |

---

## Generated artifacts summary

| File | Committed | Generated by | Regenerate with |
|---|---|---|---|
| `rtl/include/keystone86_pkg.sv` | **Yes** | `gen_from_appendix_a.py` | `make codegen` |
| `rtl/include/*.svh` | **Yes** | `gen_from_appendix_a.py` | `make codegen` |
| `microcode/tools/generators/exports/*.inc` | No — local only | `gen_from_appendix_a.py` | `make codegen` |
| `build/microcode/ucode.hex` | No — local only | `scripts/ucode_build.py` | `make ucode` |
| `build/microcode/dispatch.hex` | No — local only | `scripts/ucode_build.py` | `make ucode` |
| `build/microcode/ucode.lst` | No — local only | `scripts/ucode_build.py` | `make ucode` |
| `build/microcode/dispatch.lst` | No — local only | `scripts/ucode_build.py` | `make ucode` |

`keystone86_pkg.sv` is the authoritative RTL source for all shared constants. All RTL modules must use `import keystone86_pkg::*`. See `docs/implementation/coding_rules/source_of_truth.md`.

---

## RTL simulation source lists

The exact source files compiled for each rung are defined in the Makefile under `IVERILOG_SOURCES`, `IVERILOG_SOURCES_RUNG1`, and `IVERILOG_SOURCES_RUNG2`. Any RTL change to a file in these lists requires re-running the affected rung's simulation before claiming the baseline is still passing.

Current source list (all rungs share the same core set):

```
rtl/include/keystone86_pkg.sv
rtl/core/bus_interface.sv
rtl/core/prefetch_queue.sv
rtl/core/decoder.sv
rtl/core/microcode_rom.sv
rtl/core/microsequencer.sv
rtl/core/commit_engine.sv
rtl/core/cpu_top.sv
sim/models/bootstrap_mem.sv      (Rung 0 only)
sim/tb/tb_rung{N}_{name}.sv      (rung-specific testbench)
```

---

## Observability policy

### What observability is present

`cpu_top.sv` exports the following debug ports, which are connected to all rung testbenches:

| Port | What it observes |
|---|---|
| `dbg_eip` | Architectural EIP (primary correctness check) |
| `dbg_mseq_state` | Microsequencer state (FETCH_DECODE return check) |
| `dbg_upc` | Current micro-PC (dispatch routing check) |
| `dbg_entry_id` | Active dispatch entry ID |
| `dbg_dec_entry_id` | Decoder's current entry classification |
| `dbg_endi_pulse` | ENDI event (instruction completion signal) |
| `dbg_fault_pending` | Fault staged in commit_engine |
| `dbg_fault_class` | Fault class code |
| `dbg_decode_done` | Decoder decode_done signal |
| `dbg_fetch_addr` | Current fetch address (deadlock diagnosis) |

These are permanent observability ports. Do not remove them. They are the mechanism by which testbenches prove correctness without waveform inspection.

### What observability is not present

Verbose cycle-by-cycle trace logging is not currently active. If detailed pipeline tracing is needed during development, add it inside `synthesis translate_off` / `synthesis translate_on` guards and remove it before handing off.

Do not leave temporary instrumentation in committed RTL. See `docs/process/post_rung2_cleanup_plan.md` item 8 for the classification criteria.

### Testbench timeout behavior

All three testbenches have timeout watchdogs. On timeout, they print the current values of key debug signals before calling `$finish`. This output is the primary deadlock diagnostic. If a simulation hangs rather than reporting pass/fail, the timeout dump is the starting point for investigation.

---

## Adding a new rung simulation target

When adding Rung N to the Makefile:

1. Define `IVERILOG_SOURCES_RUNG{N}` with the correct source list.
2. Add `rung{N}-sim`, `rung{N}-regress`, and `rung{N}-clean` targets following the existing pattern.
3. Update `make clean` to include `rung{N}-clean`.
4. Add all three targets to the `help` output.
5. The `rung{N}-regress` target must run `rung{N-1}-regress` (or equivalent) before running the new testbench.
